# Core runtime
flask>=3.1
flask-cors>=6.0
flask-sqlalchemy>=3.1
flask-migrate>=4.1
sqlalchemy>=2.0
python-dotenv>=1.0
marshmallow>=4.0
langsmith>=0.3
pypdf>=5.0
# Gateway de chat real (OpenAI-compatible: OpenAI e router do Hugging Face/DeepSeek)
langchain-openai>=1.3

# Production WSGI server (replaces Flask's dev server in the container)
gunicorn>=23.0

# Dev/test
pytest>=9.0
pytest-cov>=7.0
ruff>=0.15
